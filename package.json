{
  "version": "1.0.2",
  "scripts": {
    "start": "npm run build && npx serve .",
    "dev": "npm run start",
    "render": "node tools/render-content.js",
    "build": "npm run render && node tools/update-sw-version.js",
    "bump": "npm version patch && npm run build"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
